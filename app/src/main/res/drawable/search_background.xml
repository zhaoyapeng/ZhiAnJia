<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="2dp" />
    <solid android:color="@color/userSearchInputBackgroundColor" />
    <padding
        android:left="-20dp"
        android:right="-20dp" /><!-- 各方向的间隔 -->
    <stroke
        android:width="1dp"
        android:color="@color/userSearchInputBorderColor"
        android:dashWidth="1dp" />
</shape>
